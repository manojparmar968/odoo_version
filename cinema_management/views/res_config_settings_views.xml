<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="res_config_settings_cinema_manager_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.cinema.manager.view.form.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Cinema Manager" string="Cinema Manager" name="cinema_management">
                    <block title="Movie Showings">
                        <setting id="ticket_price" string="Ticket Price" help="Sets the default price for every movie showing">
                            <field name="ticket_price" />
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_cinema_manager_configuration" model="ir.actions.act_window">
        <field name="name">Configuration</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module' : 'cinema_management'}</field>
    </record>

    <!-- Menu Item -->
    <menuitem id="menu_cinema_manager_configuration" name="Configuration"
        parent="menu_movie_root" action="action_cinema_manager_configuration"/>
</odoo>