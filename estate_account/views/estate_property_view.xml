<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_duplicate_list_view" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-success="state == 'offer_received' or state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="name"/>
                <field name="property_type_id" options="{'no_open': True, 'no_create': True}"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" optional="hide"/>
                <field name="state" readonly="0"/>
                <field name="postcode" optional="hide"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
            </list>
        </field>
    </record>
    <record id="estate_property_duplicate_form_view" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Properties">
                <header>
                    <button name="action_property_sold" string="SOLD" type="object"  invisible="state in ('canceled', 'sold')"
                        />
                    <button name="action_property_cancel" string="CANCEL" type="object" class="oe_highlight" invisible="state in ('canceled', 'sold')"/>
                    <field name="active" invisible="True"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" options="{'clickable': '1'}" readonly="1"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_invoice" type="object" class="oe_stat_button" icon="fa-pencil-square-o" invisible="invoice_count == 0">
                            <field name="invoice_count" widget="statinfo" string="Invoices"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1 class="mb32">
                            <field name="name" class="mb16"/> 
                        </h1>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color','no_create': True, 'no_create_edit': True, 'no_quick_create': True}"/>
                    </div><br/>
                    <group>
                        <group>
                            <field name="state" readonly="0"/>
                            <field name="property_type_id" options="{'no_open': True, 'no_create': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                            <field name="create_date"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="garden == False"/>
                                <field name="garden_orientation" invisible="garden == False"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted','sold', 'canceled')">
                            
                            </field>
                        </page>
                        <page string="OtherInfo">
                            <group>
                                <field name="salesman_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <record id="estate_property_duplicate_kanban" model="ir.ui.view">
        <field name="name">estate.property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id">
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bolder"/>
                        <div>
                            Mobile Number: <field name="mobile"/>
                        </div>
                        <div>
                            <strong>Expected Price: </strong><field name="expected_price"/>
                        </div>
                        <div invisible="state != 'offer_received'">
                            Best Price: <field name="best_price"/>
                        </div>
                        <div invisible="state != 'offer_accepted'">
                            <strong>Selling Price: </strong><field name="selling_price"/>
                        </div>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="estate_property_duplicate_action" model="ir.actions.act_window">
        <field name="name">Duplicate Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="view_ids" eval="[(5, 0, 0), 
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('estate_property_duplicate_kanban')}),
            (0, 0, {'view_mode': 'list', 'view_id': ref('estate_property_duplicate_list_view')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('estate_property_duplicate_form_view')})]"
        />
        <field name="context">{'search_default_available':1}</field>
        <field name="search_view_id" ref="estate.view_estate_property_filter"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Define a new Property.</p>
            <p>By New Properties.</p>
        </field>
    </record>
    <menuitem id="duplicate_parent_menu" name="Duplicate View" parent="estate.real_estate_menu_root"/>
    <menuitem id="menu_header_duplicate" name="Duplicate" parent="duplicate_parent_menu"/>
    <menuitem id="duplicate_view_menu_action" name="Properties" parent="menu_header_duplicate" action="estate_property_duplicate_action"/>
</odoo>